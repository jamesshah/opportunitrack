import { Supabase } from "../utils";

class SupabaseService {
	private constructor() {}

	static async addBackgroundJob(grantId: string) {
		// const { error } = await Supabase.getInstance()
		// 	?.from("")
		// 	.insert({ id: 1, name: "Denmark" });
		// if (error) {
		// 	throw error;
		// }
		// console.log(data);
	}
}
